# UNPACK
UNPACK - UNlearning Predictability via Activation Characterization of Knowledge


on MAC, run with PYTORCH_ENABLE_MPS_FALLBACK=1 python src/pipeline.py --config configs/config_gpt2.yaml --output outputs/ to debug (note that results are not meaningful with gpt2)